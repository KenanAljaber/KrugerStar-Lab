spring:
    application:
        name: registry-microservice
    cloud:
        config:
            uri: http://localhost:9999
logging:
  level:
    '[com.netflix.eureka.cluster]': off
    '[com.netflix.discovery.DiscoveryClient]': off